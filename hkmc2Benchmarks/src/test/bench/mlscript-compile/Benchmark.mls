import "benchmark"

let bm = benchmark

module Benchmark with...

val benchmark = bm

fun mkSuite() = new bm.Suite()

fun runSuite(suite) =
    let settings = {}
    set settings.async = false
    suite.run(settings)

// fun writeResults(path) =

